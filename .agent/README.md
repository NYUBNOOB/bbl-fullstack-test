# .agent/ — Custom Agent Tools

This folder contains custom scripts used by Claude Code (and CI) for automated testing, security verification, and project-specific workflows.

## Intended Usage (per CLAUDE.md §3)

Whenever you need to:

- Run tests
- Verify code
- Check security rules / ownership invariants

...always prefer the scripts in this directory over ad-hoc commands.

## Placeholder

This folder is scaffolded as part of project initialization. Scripts will be added as the feature set grows (e.g., adversarial test runners, ownership-invariant validators, Prisma migration checks).
