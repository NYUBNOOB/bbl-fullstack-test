import { Box, Typography } from '@mui/material';

export default function BookmarksPage() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom children="Bookmarks" />
      <Typography color="text.secondary" children="(UI pending — route is wired correctly if you see this)" />
    </Box>
  );
}
